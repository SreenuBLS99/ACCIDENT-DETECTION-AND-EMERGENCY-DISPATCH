<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>



    <application
        android:allowBackup="true"
        android:icon="@drawable/accitrack_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/accitrack_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31"
        tools:ignore="ExtraText">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".menuactivity"
            android:exported="true"/>
        <activity android:name=".MedicalDetailsActivity" />
        <activity android:name=".AddContactActivity" />
        <activity android:name=".VehicleInfoActivity" />
        <activity android:name=".VehicleDetailsActivity"/>
        <activity android:name=".ViewContactsActivity" />
        <activity android:name=".DeleteContactsActivity" />

    </application>

</manifest>
